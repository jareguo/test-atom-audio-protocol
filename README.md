# test-atom-audio-protocol

This repo tests the problem that atom-shell's protocol can not worked with `<audio>`.

The `<img>` in html can load from `library://` protocol successfully, but `<audio>` can not.
